# Multi-Agent Learning Chat API - Environment Configuration
# Copy this file to .env and fill in your actual values

# ============================================================================
# REQUIRED CONFIGURATION
# ============================================================================

# Google Gemini API Key (REQUIRED)
# Get your API key from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# ============================================================================
# OPTIONAL CONFIGURATION (with defaults)
# ============================================================================

# API Version
# Default: v1
# API_VERSION=v1

# Performance Settings
# Maximum timeout for AI generation requests (in seconds)
# Default: 15, Range: 1-60
# REQUEST_TIMEOUT=15

# Maximum length for user messages (in characters)
# Default: 5000, Range: 1-10000
# MAX_MESSAGE_LENGTH=5000

# Concurrency Settings
# Maximum concurrent AI requests (semaphore limit)
# Increase for higher throughput, decrease if hitting API rate limits
# Default: 300, Range: 10-1000
# MAX_CONCURRENT_REQUESTS=300

# Thread pool size for blocking Gemini API calls
# Should be >= MAX_CONCURRENT_REQUESTS for optimal performance
# Default: 500, Range: 10-2000
# THREAD_POOL_WORKERS=500

# Server Settings
# Allowed CORS origins (comma-separated list or ["*"] for all)
# Default: ["*"]
# For production, specify exact origins:
# CORS_ORIGINS=["https://yourdomain.com","https://app.yourdomain.com"]
# CORS_ORIGINS=["*"]

# Logging Level
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
# Default: INFO
# LOG_LEVEL=INFO

# Rate Limiting Settings
# Enable or disable rate limiting middleware
# Default: True
# RATE_LIMIT_ENABLED=True

# Maximum requests per IP address per minute
# Default: 100, Range: 1-10000
# RATE_LIMIT_PER_IP=100

# Maximum total requests per minute (global limit)
# Default: 1000, Range: 1-100000
# RATE_LIMIT_GLOBAL=1000

# Application Metadata
# Application name
# Default: Multi-Agent Learning Chat API
# APP_NAME=Multi-Agent Learning Chat API

# Application version
# Default: 1.0.0
# APP_VERSION=1.0.0
